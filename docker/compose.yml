services:
  climate-agent:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: gaon-climate-edge:latest
    container_name: gaon-climate-edge
    restart: unless-stopped
    volumes:
      - ${APP_CONFIG_DIR:-../config}:/app/config:ro
      - ${APP_LOG_DIR:-../log}:/app/log
      - ${APP_DEVICE_CONFIG_DIR:-/home/doublej/.config/gaon-climate}:/root/.config/gaon-climate
    privileged: true
    devices:
      - /dev/gpiomem:/dev/gpiomem
      - /dev/mem:/dev/mem
    command: ["--mode", "${CLIMATE_MODE:-local}"]
